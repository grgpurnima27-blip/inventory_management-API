{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "releaseCommand": "python manage.py migrate && python create_superuser.py && python manage.py loaddata accounts.json && python manage.py loaddata warehouses.json && python manage.py loaddata products.json && python manage.py loaddata inventory.json && python manage.py loaddata coupons.json && python manage.py loaddata reviews.json && python manage.py loaddata wishlist.json",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}